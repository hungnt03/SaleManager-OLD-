<UserControl x:Class="SaleManager.WPF.Views.Template.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SaleManager.WPF.Views.Template"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="personTemplate">
            <StackPanel Orientation="Horizontal" Height="22" >
                <materialDesign:PackIcon Kind="{Binding Path=Icon}" Width="30" Height="20" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Path=LabelName}" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto" DockPanel.Dock="Left">
        <ListBox ItemsSource="{Binding Menus}" ItemTemplate="{StaticResource personTemplate}" 
                 IsEnabled="{Binding IsChecked, ElementName=EnableListBox}"
                 SelectedItem = "{Binding MenuSelected}">

        </ListBox>
    </ScrollViewer>
</UserControl>
